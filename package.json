{
  "name": "butter",
  "version": "1.0.0",
  "description": "🧈 Makes your webpage loading smooth and animated ",
  "repository": "git@github.com:mvoloskov/butter.git",
  "main": "src/index.js",
  "author": "mvoloskov",
  "license": "BSL-1.0",
  "scripts": {
    "build-iife": "rollup -c",
    "watch": "rollup -c --watch",
    "serve": "serve",
    "dev": "concurrently 'npm run watch' 'npm run serve'"
  },
  "husky": {
    "hooks": {
      "pre-push": "npm run build-iife && git add dist"
    }
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "husky": "^4.2.5",
    "rollup": "^2.26.10",
    "serve": "^11.3.2"
  }
}
